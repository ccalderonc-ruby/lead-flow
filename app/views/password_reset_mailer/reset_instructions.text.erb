Hi <%= @user.name.split(/\s+/).first %>,

We received a request to reset your LeadFlow password. Open this link to choose a new password:

<%= @reset_url %>

This link expires in <%= @expires_in_hours %> hours. If you did not request a reset, you can ignore this email.

— LeadFlow
